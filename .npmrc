# Ensure proper permissions for binaries
unsafe-perm=true

# Install all dependencies including devDependencies
production=false

# Avoid native module compilation issues
build-from-source=false

# Explicitly exclude bcrypt
bcrypt=false 